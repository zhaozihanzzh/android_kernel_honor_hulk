
config HW_DOUBLE_FREE_DYNAMIC_CHECK
	bool "hw double free dynamic check"
	default n
	help
	  function depends on SLUB.
	  support double free check function.
	  upload the call stack information to.
	  hisecd when double free happen.
